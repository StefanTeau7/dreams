type Dream {
  id: ID
  subject: String
  user: User
}

type User {
  id: ID
  first: String
  last: String
  phone: String
  email: String
  password: String
}

type Chat @model @auth(rules: [{allow: public}]) {
  id: ID!
  text: String
  dream: Dream
}
 